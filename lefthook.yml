pre-commit:
  jobs:
    - name: "code format"
      run: bun run fmt {staged_files}
      staged_files: true
    - name: "lint fix"
      glob: "**/*.{ts}"
      run: bun run lint
      staged_files: true
    - name: "unit test"
      glob: "src/**/*.{ts}"
      run: bun run test
